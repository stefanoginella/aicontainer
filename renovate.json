{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended", ":dependencyDashboard"],
  "labels": ["dependencies"],
  "schedule": ["before 6am on Monday"],
  "packageRules": [
    {
      "matchManagers": ["dockerfile"],
      "matchFileNames": ["template/Dockerfile"],
      "commitMessageTopic": "{{depName}} (Dockerfile)"
    }
  ],
  "customManagers": [
    {
      "description": "Keep the ARG *_VERSION pins in sync across both Dockerfiles. .devcontainer/Dockerfile is a build-mode copy of template/Dockerfile; the built-in dockerfile manager already bumps FROM digests in both, so scope this regex manager to both too — otherwise fzf/git-delta bumps land only in template/ and the dogfood drifts every cycle (reconciled previously by a manual `aic sync`).",
      "customType": "regex",
      "managerFilePatterns": ["/^template/Dockerfile$/", "/^\\.devcontainer/Dockerfile$/"],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>.*?) depName=(?<depName>.*?)(?: registryUrl=(?<registryUrl>.*?))?\\s+ARG \\w+_VERSION=(?<currentValue>.*)\\s"
      ]
    },
    {
      "description": "Keep the CI smoke assertion's hardcoded socket-proxy pin in sync with the compose files. rebuild.yml and release.yml assert the exact digest-pinned image because that service holds the raw host socket, so a compose-only bump fails the smoke test every cycle.",
      "customType": "regex",
      "managerFilePatterns": ["/^\\.github/workflows/(rebuild|release)\\.yml$/"],
      "matchStrings": [
        "(?<depName>tecnativa/docker-socket-proxy):(?<currentValue>v[^@'\\s]+)@(?<currentDigest>sha256:[a-f0-9]{64})"
      ],
      "datasourceTemplate": "docker"
    }
  ]
}
