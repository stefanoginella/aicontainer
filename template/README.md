<!--
  This file is generated by aicontainer (`aic init` / `aic sync`) and is itself
  overwritten on every sync. To change it, edit template/README.md upstream:
  https://github.com/stefanoginella/aicontainer
-->

# `.devcontainer/` — what to edit (and what not to)

This directory is managed by [**aicontainer**](https://github.com/stefanoginella/aicontainer).
Some files here are **regenerated from scratch every time you run `aic init` or
`aic sync`** — editing them is pointless, your changes are silently lost on the
next sync. Others are **project-owned**: aic never touches them, so that's where
per-project customization belongs.

> **🤖 AI agents — read this first.** Do **not** edit `devcontainer.json` or
> `docker-compose.yml` to customize this container. Those files are overwritten
> on the next `aic sync`, and inside the container everything under
> `.devcontainer/` is read-only and blocked by the PreToolUse hook anyway — your
> write will fail. To customize, ask the user to create one of the
> **project-owned files** below **on the host**, then `aic sync` + `aic rebuild`.

## ❌ Managed by aic — do NOT edit (overwritten on `aic sync`)

| File | What it is |
| --- | --- |
| `devcontainer.json` | Dev Container config. Only `containerEnv.AIC_TOOLS` / `AIC_SHELL` and the project label survive a sync. |
| `docker-compose.yml` | Compose stack (devcontainer + socket-proxy). Image tag is pinned to your aic version. |
| `README.md` | This file. |
| `Dockerfile` *(build mode only)* | The sandbox image definition. |
| `post-create.py` *(build mode only)* | Container-creation setup. |
| `hooks/` *(build mode only)* | PreToolUse security hooks (Claude + Codex). |
| `aic-firewall`, `aic-chown-volumes`, `aic-lock-gitconfig` *(build mode only)* | Privileged helper scripts. |
| `.zshrc` *(build mode only)* | Baked login-shell rc. |

In **pull mode** only `devcontainer.json`, `docker-compose.yml`, and this
`README.md` are present; the rest live inside the published image. To change a
managed file, edit the upstream `template/` in the aicontainer repo and run
`aic sync` — or, for the common cases, use a project-owned file below.

## ✅ Project-owned — safe to create & edit (survives `aic sync`)

Each is **opt-in by presence** (you create it; aic reads it only if it exists)
and **read-only inside the container** — so create/edit them **on the host**.
Run `aic sync && aic rebuild` after adding one.

| File | Use it to customize | aicontainer README section |
| --- | --- | --- |
| `Dockerfile.project` | Extra OS packages / tools layered on the base image | *Installing extra tools* |
| `docker-compose.override.yml` | env vars, mounts, named volumes, `extra_hosts`, a `build:` for `Dockerfile.project` | *docker-compose.override.yml* |
| `firewall-allowlist` | Extra outbound hosts allowed when the firewall is enabled (one host per line) | *Firewall* |
| `chown-paths` | Named-volume mountpoints to re-own to `vscode` (one path per line) | *Persisting a named volume* |
| `post-create.project.sh` | Commands to run on every container create (`lefthook install`, `npm ci`, …) | *Project-specific post-create steps* |
| `vscode-extensions` | VS Code editor extensions to auto-install (one `publisher.name` id per line) | *Project-specific VS Code extensions & settings* |
| `vscode-settings.json` | VS Code editor settings (a JSON object) | *Project-specific VS Code extensions & settings* |

Full docs: <https://github.com/stefanoginella/aicontainer#readme>

> **Need to override a setting aic already sets in `devcontainer.json`** (e.g. a
> `terminal.*` value)? Put it in a workspace **`.vscode/settings.json`** at the
> repo root (not here). Workspace settings beat the container's machine-scope
> settings, survive sync, and aren't aic-managed.
